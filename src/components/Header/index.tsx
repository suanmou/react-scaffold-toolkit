import React from 'react'
import { Button } from 'antd'

import styles from './style.less'

const Header = () => (
  <header className={styles.header}>
    哈哈哈111<Button type="primary">ggggg</Button>
  </header>
)

export default Header
